[project]
name = "moralquotes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "aumrao", email = "aumrao37@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "openai-agents>=0.21.1",
]

[project.scripts]
moralquotes = "moralquotes:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
